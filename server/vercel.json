{
  "version": 2,
  "builds": [
    { "src": "server.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/create", "dest": "/server.js" },
    { "src": "/employees", "dest": "/server.js" },
    { "src": "/(.*)", "dest": "/server.js" }
  ]
}

